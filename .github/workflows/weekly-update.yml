name: Weekly Update

on:
  schedule:
    - cron: 0 0 * * 0
  workflow_dispatch:

jobs:
  ci:
    name: CI
    permissions:
      pages: write
      id-token: write
    uses: ./.github/workflows/ci.yml